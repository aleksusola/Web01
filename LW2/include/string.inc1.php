<?php
    function Last ($str, $defaultValue = '')
    {
        $result = $defaultValue;
        if (isset($_GET[$paramName]))
        {
            $result = $_GET[$paramName]);
        }
        return $result
    }
    function WithoutLast ($str, $defaultValue = '')
    {
        $result = $defaultValue;
        if (isset($_GET[$paramName]))
        {
            $result = $_GET[$paramName]);
        }
        return $result
    }
    function Revers ($str, $defaultValue = '')
    {
        $result = $defaultValue;
        if (isset($_GET[$paramName]))
        {
            $result = $_GET[$paramName]);
        }
        return $result
    }
    